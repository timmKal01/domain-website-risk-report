{
    "actorOutputSchemaVersion": 1,
    "title": "Output schema of Domain & Website Risk Report",
    "description": "One record per domain: registration status, SSL health, discovered subdomains, tech stack, and computed risk flags.",
    "properties": {
        "results": {
            "type": "string",
            "title": "Results",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
